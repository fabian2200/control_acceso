name: control_acceso_flutter
description: Kiosko táctil de control de acceso (offline-first, SQLite).
publish_to: "none"
version: 2.4.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  sqflite_common_ffi: ^2.3.6
  sqlite3_flutter_libs: ^0.5.32
  path: ^1.9.1
  http: ^1.4.0
  connectivity_plus: ^6.1.5
  camera: ^0.11.2
  image: ^4.5.4
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
